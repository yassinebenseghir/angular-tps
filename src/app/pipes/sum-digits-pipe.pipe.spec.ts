import { SumDigitsPipePipe } from './sum-digits.pipe';

describe('SumDigitsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SumDigitsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
