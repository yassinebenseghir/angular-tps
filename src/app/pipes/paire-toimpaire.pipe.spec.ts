import { PaireToimpairePipe } from './paire-toimpaire.pipe';

describe('PaireToimpairePipe', () => {
  it('create an instance', () => {
    const pipe = new PaireToimpairePipe();
    expect(pipe).toBeTruthy();
  });
});
